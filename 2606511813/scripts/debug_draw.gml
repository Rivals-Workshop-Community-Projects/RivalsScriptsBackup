//debug-draw
/*
var playerJoyDir = joy_dir;
if (playerJoyDir > 180) {
    playerJoyDir -= 360;
}

draw_debug_text(x, y, string(playerJoyDir));
*/

/*
with pHitBox {
    draw_debug_text(x, y, string(darctan2(-vsp, hsp)));
    //draw_debug_text(x, y+20, string(trailArray));
}
*/

//draw_debug_text(x, y, string(arrowTrailArray));

//draw_debug_text(x, y, string(window));
//draw_debug_text(x, y, "J");

//draw_debug_text(x, y, string(floor(fps_real/10)*10))