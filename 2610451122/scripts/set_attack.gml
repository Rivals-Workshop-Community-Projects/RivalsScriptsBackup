//set_attack

//there is not a single tutorial on how to set an attack index fuck you

//secret taunt totally not stolen from The mongus
if (prev_state == PS_IDLE && state == PS_ATTACK_GROUND && attack == AT_TAUNT){
    attack = AT_TAUNT_2;
}