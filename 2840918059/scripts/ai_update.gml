//Uspecial Direction
if (ai_recovering && attack == AT_USPECIAL && window == 2 && spr_dir == 1){
	up_down = true;
	right_down = true;
}

if (ai_recovering && attack == AT_USPECIAL && window == 2 && spr_dir == -1){
	up_down = true;
	left_down = true;
}
