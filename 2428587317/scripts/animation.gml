//switch (state){
//    case PS_IDLE:
        //code here can change the sprite_index and image_index while in the idle state
//    break;
    
//    default: break;
//}
//if (was_parried && state != PS_ATTACK_GROUND && state != PS_ATTACK_AIR) {
if (state == PS_PRATLAND) {
    sprite_index = sprite_get( "landinglag" );
}
