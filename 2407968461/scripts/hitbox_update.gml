if (attack == AT_USTRONG){
    if (hitbox_timer == length - 1){
        spawn_hit_fx( x + 12*spr_dir, y + 2, 144 );
    }
}

if (attack == AT_FSPECIAL){
    if (hitbox_timer == length - 1){
        spawn_hit_fx( x + 12*spr_dir, y + 2, 144 );
    }
}