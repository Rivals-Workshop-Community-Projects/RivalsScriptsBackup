if (my_hitboxID.attack == AT_USTRONG && my_hitboxID.hbox_num = 2 || 3)
{
    //hit_player_obj.fall_through = false;
    if (!free) hit_player_obj.y -= 1;
}

if !(my_hitboxID.attack == AT_USTRONG && hit_player = player) turnabout_timer_pause = 0;
