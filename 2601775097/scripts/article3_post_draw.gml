//article3 post_draw
//power smash article

//see collisionbox
if (get_match_setting(SET_HITBOX_VIS))
{
    if (state == 0) draw_sprite_ext(sprite_get("artc_powersmash_mask"), 0, x, y, 1, 1, 0, c_white, 0.5);
}