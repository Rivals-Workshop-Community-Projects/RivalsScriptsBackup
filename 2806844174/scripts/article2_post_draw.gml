// Article Update is ran for every frame that the article object is in-game.

if player_id.poggers_alt draw_sprite_ext(sprite_get("wheel_pog"), image_index, x, y, spr_dir, 1, 0, player_id.draw_color, 1);