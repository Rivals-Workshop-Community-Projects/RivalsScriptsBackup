//Changer Object Update
var is_hitbox = (object_index == pHitBox);
switch(changer_type) {
	case "Ice Block":
		var ice_xscale = is_hitbox?(image_xscale * 100):image_xscale, ice_yscale = is_hitbox?(image_yscale * 100):image_yscale;
		
		
	break;
}