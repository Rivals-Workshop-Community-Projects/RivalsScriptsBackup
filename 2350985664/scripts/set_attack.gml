//set_attack

if (attack = AT_USPECIAL)
{
    if (has_uspecial > 0)
    {
        has_uspecial -= 1;
    }
}

if (attack = AT_FSPECIAL)
{
    if (has_fspecial > 0)
    {
        has_fspecial -= 1;
    }
}