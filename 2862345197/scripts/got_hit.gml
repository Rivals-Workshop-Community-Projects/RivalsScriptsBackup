if orig_knock > 0 {
sound_stop(sound_get("laser_charge"));
    if popportunity = true && !has_rune("D") {
    sound_play(sound_get("bubble_leave"));
    spawn_hit_fx( x, y, pop_fx);
	popportunity = false;
    }
}

// disconnect from node
user_event(3);