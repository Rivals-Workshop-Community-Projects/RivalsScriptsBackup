//article1_update
image_xscale = portal_min_scale;
image_yscale = portal_min_scale;

/*with(owner)
{
    if(attack == AT_NSPECIAL && (state == PS_ATTACK_GROUND || state == PS_ATTACK_AIR))
    {
        if(window = 1 && window_timer == 1)other.instance_destroy();
    }
}*/