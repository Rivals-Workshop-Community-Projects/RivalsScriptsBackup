testvar = false;
/*
if (create_beam) {
    var alpha = 1;
    if (beam_timer > 20) {
        alpha = 1 - (beam_timer - 20)/15;
    }
    draw_sprite_ext(sprite_get("blaster_beam"), clamp(floor(beam_timer/3), 0, 6), x, y, scaleX, scaleY, angleEnd, c_white, alpha);
}
*/