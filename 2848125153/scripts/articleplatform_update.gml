//article1_update
if(!instance_exists(choochoo)){
    instance_destroy();
    exit;
}

/*with(oPlayer){
			if(place_meeting(x,y,other) && !free && !freemd){
				hsp = other.hsp;vsp = other.vsp;
			}
		}*/