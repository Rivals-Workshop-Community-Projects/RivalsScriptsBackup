//draw hud

/*

    var mul = 16; //spacing
    

    draw_debug_text(temp_x+mul*5, temp_y+mul*-8, "flashbang timer: " + string(flashbang_detonate_timer) + "/" + string(flashbang_detonate_max));
 
      
    draw_debug_text(temp_x+mul*5, temp_y+mul*-4, "state = " + string(get_state_name(state)));
    draw_debug_text(temp_x+mul*5, temp_y+mul*-3, "state_timer = " + string(state_timer));

    if (
        is_attacking || state == PS_PARRY || state == PS_ROLL_FORWARD || state == PS_ROLL_BACKWARD ||
        state == PS_TECH_FORWARD || state == PS_TECH_BACKWARD || state == PS_AIR_DODGE)
    {
        if (is_attacking) draw_debug_text(temp_x+mul*0, temp_y+mul*-7, "attack = " + string(attack_names[attack]));
        draw_debug_text(temp_x+mul*5, temp_y+mul*-6, "window = " + string(window));
        draw_debug_text(temp_x+mul*5, temp_y+mul*-5, "window_timer = " + string(window_timer));
    }


    draw_debug_text(temp_x+mul*5, temp_y+mul*-2, "x = " + string(x));
    draw_debug_text(temp_x+mul*5, temp_y+mul*-1, "y = " + string(y));

    draw_debug_text(temp_x+mul*10, temp_y+mul*-2, "hsp = " + string(hsp));
    draw_debug_text(temp_x+mul*10, temp_y+mul*-1, "vsp = " + string(vsp));
*/

