//Rune new attacks

if (attack == AT_UTILT && has_rune("D")){		// Original Up Tilt
	attack = 40;		// picking 40 as it is an empty slot
}

if (attack == AT_BAIR && has_rune("J")){		// Original Back Air with Extra Strength
	attack = 42;		// picking 42 as it is an empty slot
}
