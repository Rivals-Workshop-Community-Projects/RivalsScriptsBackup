user_event(13);

if (attack == AT_USPECIAL && butterfly != noone){      //Uses the USpecial Teleport if the butterfly is out
    attack = AT_USPECIAL_2;
    
}

set_attack_value(AT_FSTRONG, AG_CATEGORY, 0);
set_attack_value(AT_FSTRONG, AG_OFF_LEDGE, 0);