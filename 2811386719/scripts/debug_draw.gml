
// draw_debug_text( x+60, y-30, "CAN_ CLING : " + string(can_cling));
// draw_debug_text( x+10, y, "x : " + string(x));
// draw_debug_text( x+60, y, "y : " + string(static_cooldown));

// if(get_match_setting(SET_HITBOX_VIS)){
//     with(pHitBox){
//         // draw_debug_text( x, y, "transcendent : " + string(transcendent));
//         // draw_debug_text( x, y-15, "plasma_safe : " + string(plasma_safe));
//         if(player_id == other and attack == AT_FSTRONG and hbox_num == 3){
//             draw_sprite_ext(mask_index, image_index, x, y, set_image_xscale, image_yscale, image_angle, c_white, 0.5);
            
//         }
//     }
// }
// var len = array_length(static_people);
// for(var i = 0; i < len; i++){
//   var sp = static_people[i];
//   if(instance_exists(sp) and sp.static_pull > 0){
    
//     draw_debug_text( floor(sp.x-60), floor(sp.y-15), "static : " + string(sp.static_pull));
//   }
 
// }
