if("ring_out" not in self) exit;

//draw_debug_text(x, y + 10, string(ring_timer));
//draw_debug_text(x, y + 25, string(ring_equipped));

/*if(ring_out != noone){
    with(ring_out){
    draw_debug_text(x, y + 10, string(state));
    }
}//*/