///#args attack // this line makes code editors not freak out for some reason

uspecial_direction_arr = [90];

// win = get_attack_value(attack, AG_NUM_WINDOWS)
// set_window_value(attack, win, AG_WINDOW_TYPE, 7 * (aerial_pratfall_timer > 0));


// MunoPhone Touch code - don't touch
// should be at BOTTOM of file, but above any #define lines
muno_event_type = 2;
user_event(14);
