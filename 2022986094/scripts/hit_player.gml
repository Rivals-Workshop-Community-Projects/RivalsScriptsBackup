//hit_player - called when one of your hitboxes hits a player
if ((attack == AT_USPECIAL && (window == 2 || window = 3)) && has_hit_player == true) {
    hit_player_obj.hitstop = 15;
    hit_player_obj.hitstop_full = 15;
    hit_player_obj.hitpause = true;
    if ((window = 2 || window = 3) || window = 6){
    x = hit_player_obj.x - spr_dir * 25
    y = hit_player_obj.y 
    }
}


