with (obj_article3) {
    if player_id == other.id {
        //draw_debug_text(x, y, `State: ${x}`);
        //draw_debug_text(x, y + 20, `State: ${room_width}`);
    }
}
//draw_debug_text(x, y, `State: ${hit_player_obj}`);
//draw_debug_text(x, y, `State: ${closestEnemy.x}`);
with (obj_article2) {
    if player_id == other.id {
        //draw_debug_text(x, y, `locked: ${id}`);
        
        //draw_debug_text(x, y, `locked: ${lockedDirection}`);
        //draw_debug_text(x, y+20, `forward: ${bulletForward}`);
        //draw_debug_text(x, y+40, `dir: ${spr_dir}`);
    }
}
//draw_debug_text(x, y+10, string(spr_dir));


//draw_debug_text(x, y, `State: ${y}`);

//draw_debug_text(x, y+10, string(spr_dir));


//draw_debug_text(x, y, `State: ${previousRockID}`);
//draw_debug_text(x, y+20, `State: ${previousFlameID}`);
