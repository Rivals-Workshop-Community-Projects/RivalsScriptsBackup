


// var cur_x;
// var cur_y;

// if (instance_exists(cursor_id)){

// 	cur_x	= get_instance_x(cursor_id);
// 	cur_y = get_instance_y(cursor_id);



// 	if (point_in_rectangle(cur_x, cur_y, x + button_x, y + button_y, x + button_x + 32, y + button_y + 32)){
// 		suppress_cursor = true;
// 			if (menu_a_pressed){
// 			TK_button = !TK_button;
// 			sound_play(asset_get("mfx_coin"));
// 			}
// 	}

// 	set_synced_var( player, TK_button );

// }


// #define point_in_rectangle(px, py, x1, y1, x2, y2)
// {
//   return px >= x1 && px <= x2 && py >= y1 && py <= y2;
// }
