sprite_index = asset_get("sprite_empty")
mask_index = sprite_get("a");

bite_timer_rn = 0;
bite_timer_max = 450 - 200 + random_func(10,400,true); //how long it takes until moncjh in frames (semi-random, around 400 frames variation)