//hitbox_update

if (attack == AT_JAB) {


//var dist = room_width * room_height;
//with oPlayer {
//	if point_distance(x, y, other.x, other.y) <= dist && state != PS_DEAD {
//	other.ai_target = id;
//	dist = point_distance(x, y, other.x, other.y);
//	}
//}

//set_hitbox_value( AT_JAB, 3, HG_PROJECTILE_HSPEED, 0+ai_target.x);
//set_hitbox_value( AT_JAB, 3, HG_PROJECTILE_VSPEED, 0+ai_target.y);
}


if (attack == AT_NSPECIAL) {

	if(hbox_num == 2){

	proj_angle += 1

	}
}

if (attack == AT_USTRONG) {

	if(hbox_num == 3){
				/*
				if(free == false){
				set_state(PS_WAVELAND)
				invincible = false
				invince_time = 0
				}else{
				set_state(PS_PRATFALL)
				invincible = false
				invince_time = 0
				}
				*/
	
	}

}