if (firstattack && attack == AT_DSTRONG && !hit_player_obj.free)
{
	hit_player_obj.hitstop = 40;
}



if (star_meter < 100) && attack != AT_EXTRA_2
{
	if !install
	star_meter += my_hitboxID.damage * 1.4;
	else
	star_meter += my_hitboxID.damage * 0.5;
}
if (star_meter > 100)
star_meter = 100;


if (attack = 49)
hit_player_obj.should_make_shockwave = false;
